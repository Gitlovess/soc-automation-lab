<!-- Custom Wazuh Detection Rules -->
<!-- Path: /var/ossec/etc/rules/local_rules.xml -->

<group name="local,syslog,sshd,">

  <!-- Rule 100001: SSH Authentication Failure -->
  <rule id="100001" level="5">
    <if_sid>5716</if_sid>
    <srcip>1.1.1.1</srcip>
    <description>sshd: authentication failed from IP 1.1.1.1</description>
    <group>authentication_failed,pci_dss_10.2.4,pci_dss_10.2.5,</group>
  </rule>

  <!-- Rule 100002: Mimikatz Usage Detection -->
  <!-- Triggers on Sysmon Event 1 (Process Creation) -->
  <!-- Matches mimikatz.exe filename (case-insensitive) -->
  <!-- MITRE ATT&CK: T1003 - OS Credential Dumping -->
  <rule id="100002" level="15">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)mimikatz\.exe</field>
    <description>Mimikatz Usage Detected</description>
    <mitre>
      <id>T1003</id>
    </mitre>
  </rule>

</group>
